def missing_num(list):
    list.sort()
    l = len(list)
    for x in range (0,l):
        if list[x+1]-list[x] != 1:
            return x+2
        elif max(list) == 9:
            return 10
        elif min(list) == 2:
            return 1

